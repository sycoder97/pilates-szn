{
  "name": "pilates-szn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "London pilates studio directory with auto-updated pricing.",
  "scripts": {
    "refresh": "node scripts/refresh-pricing.mjs",
    "refresh:dry": "node scripts/refresh-pricing.mjs --dry-run",
    "refresh:one": "node scripts/refresh-pricing.mjs --only"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  }
}
